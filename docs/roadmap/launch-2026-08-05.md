# 兑一兑 · 2026-08-05 上线倒计时

> **上线日：** 2026-08-05（周三）  
> **目标：** 公网 Web 邀请制开放，宣布「开始收使用反馈」  
> **对照：** [f3-launch-checklist.md](./f3-launch-checklist.md) · [fast-launch.md](./fast-launch.md)  
> **态势：** [agents/STATUS.md](./agents/STATUS.md)  
> **维护：** 总负责人  

口号：对自己守信，才能对他人守信。 · 做到了，兑一兑。

---

## 0. 上线定义（8/5 当天要达成）

1. `https://<Web域名>` 可打开注册/登录  
2. `https://<API域名>/api/health` 返回 ok（含 DB）  
3. 手机浏览器走通：注册 → 建「我的」承诺 → 达成 → 兑奖 → 意见反馈  
4. 邀请制：`REGISTRATION_INVITE_CODE` 已开（或书面确认开放注册）  
5. 值班：`FEEDBACK_ADMIN_KEY` + [feedback-duty.md](./decisions/feedback-duty.md) 填实  
6. 对外一句：**初版 Web 已上线，开始收使用反馈**  
7. 首批邀请名单就绪（目标 ≥20，见 [promotion-plan.md](./feedback/promotion-plan.md)）

**不做（不挡 8/5）：** 真邮件 SMTP、App、盈利、终极版功能。

---

## 1. 倒计时（按日）

### 8/2（六）· 锁定口径与清单 — **今天**

| # | 动作 | 谁 | 状态 |
|---|------|----|------|
| A1 | 冻结本文件为上线唯一日历 | 总负责人 | 进行中 |
| A2 | 仓库 `main` 保持可构建；`npm test` / 本地 smoke 绿 | 总负责人 | 已具备 |
| A3 | 部署脚本兼容 `docker compose` / `docker-compose` | 总负责人 | 进行中 |
| A4 | **用户确认：** 域名、云主机、部署方式（见 §2） | 用户 | **待填** |

### 8/3（日）· 主机 + Staging

| # | 动作 | 谁 | 状态 |
|---|------|----|------|
| B1 | 云主机就绪；开放 80/443（及 SSH） | 用户 | 待 |
| B2 | DNS：`app` / `api`（或同域反代）A 记录指到主机 | 用户 | 待 |
| B3 | 主机上 `git clone` / pull `main`，填生产 `.env` | 总负责人+用户 | 待 |
| B4 | `npm run deploy:staging` 或 Compose 起服务；本机/内网 smoke | 总负责人 | 待 |
| B5 | `npm run backup:db` 试跑；确认数据卷路径 | 总负责人 | 待 |

### 8/4（一）· HTTPS + 预演

| # | 动作 | 谁 | 状态 |
|---|------|----|------|
| C1 | Caddy/Nginx：加载 [deploy/Caddyfile](../../deploy/Caddyfile)（改真实域名） | 总负责人+用户 | 待 |
| C2 | `.env` 全量 `https://…` 后 **重建 web** | 总负责人 | 待 |
| C3 | 手机公网主闭环预演；勾 [f3-launch-checklist](./f3-launch-checklist.md) 公网列 | 总负责人 | 待 |
| C4 | 设 `REGISTRATION_INVITE_CODE`；准备邀请短链 + 话术 | 总负责人+用户 | 待 |
| C5 | 填 [invite-list.template.md](./feedback/invite-list.template.md) 目标名单 | 用户 | 待 |

### 8/5（三）· 正式开放

| # | 动作 | 谁 | 状态 |
|---|------|----|------|
| D1 | 最终 smoke + health | 总负责人 | 待 |
| D2 | 发出首批邀请（≥20 目标，当天发出即可） | 用户为主 | 待 |
| D3 | 宣布开始收反馈（私聊/群/可选朋友圈） | 用户 | 待 |
| D4 | 勾完 F3 闸门；周报开一条「上线日记录」 | 总负责人 | 待 |
| D5 | `feedback-ops` 进入值班节奏（周五 digest） | feedback-ops | 待 |

---

## 2. 上线阻塞项（须你拍板）

没有下列信息，**无法在 8/5 完成公网 HTTPS**：

| 项 | 请提供 |
|----|--------|
| **域名** | Web / API 域名（例：`app.xxx.com` + `api.xxx.com`，或同域路径） |
| **云主机** | 厂商、公网 IP、SSH 登录方式（或面板） |
| **运行时** | 主机上 Docker Engine（推荐）或本机 Docker Desktop/OrbStack/Colima 再反代 |
| **联系邮箱** | `SUPPORT_EMAIL`（忘记密码文案） |
| **邀请码** | 拟定一串 `REGISTRATION_INVITE_CODE`（建议开启） |

本机现状（2026-08-02）：仅有 Docker **CLI** + `docker-compose`，**daemon 未运行**；公网上线优先在**云主机**装 Docker 部署。

---

## 3. 生产 `.env` 必填清单

复制自 `.env.example`，上线前逐项确认：

| 变量 | 上线值 |
|------|--------|
| `JWT_SECRET` | 强随机（已本地可有，**勿提交 git**） |
| `APP_URL` / `FRONTEND_ORIGIN` | `https://<Web>` |
| `NEXT_PUBLIC_API_URL` | `https://<API>`（改后必须重建 web） |
| `EXPOSE_RESET_URL` | `false` |
| `SUPPORT_EMAIL` | 真实可收信邮箱 |
| `FEEDBACK_ADMIN_KEY` | 强随机；值班拉取用 |
| `REGISTRATION_INVITE_CODE` | **建议设置**（邀请制） |
| `NODE_ENV` | `production` |

---

## 4. 回滚与值班（上线日）

| 场景 | 动作 |
|------|------|
| 部署失败 | 保留上一镜像/容器；先修 `.env` / Caddy，勿清数据卷 |
| 主闭环挂 | 暂停发邀请；修 API 后 `smoke` 再开 |
| 反馈爆炸 | 只记 P0；其余进周五 digest |
| 数据 | 上线前与每日 `backup:db` 或卷快照 |

值班口径见 [feedback-duty.md](./decisions/feedback-duty.md)。

---

## 5. 完成判定

- [ ] §0 七条全部满足  
- [ ] [f3-launch-checklist.md](./f3-launch-checklist.md) 公网列 #1～#12 勾选  
- [ ] [STATUS.md](./agents/STATUS.md) 标记「F3 已开放」  
- [ ] 推广窗按 [promotion-plan.md](./feedback/promotion-plan.md) 执行（窗口以 8/5 起算）  
